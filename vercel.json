{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "trigger rebuild with clean code - all TS errors fixed",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
